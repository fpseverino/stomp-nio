name: CI

on:
   push:
     branches:
     - main
   pull_request:
     branches:
     - main
   release:
     types: [published]
   workflow_dispatch:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-ci
  cancel-in-progress: true

jobs:
  linux-unit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      matrix:
        tag:
          - swift:6.2
    container:
      image: ${{ matrix.tag }}
    services:
      rabbitmq:
        image: rabbitmq:3.13
        ports:
          - 61613:61613
        options: >-
          --health-cmd "rabbitmq-diagnostics -q ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
        volumes:
          - ${{ github.workspace }}/.github/rabbitmq:/etc/rabbitmq:ro
    steps:
    - name: Checkout
      uses: actions/checkout@v6
    - name: Wait for STOMP port
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        set -e
        apt-get -qq update
        apt-get -yqq install netcat-openbsd
        for i in {1..120}; do
          if nc -z rabbitmq 61613; then
            echo "STOMP port is up"
            exit 0
          fi
          sleep 1
        done
        echo "STOMP port did not become ready in time" >&2
        exit 1
    - name: Test
      env:
        STOMP_SERVER: rabbitmq
      run: swift test

  lint:
    runs-on: ubuntu-latest
    container: swift:noble
    timeout-minutes: 5
    steps:
      - name: Configure git
        run: git config --global --add safe.directory "${GITHUB_WORKSPACE}"
      - name: Check out code
        uses: actions/checkout@v6
      - name: Lint
        run: |
          apt -q update && apt -yq install curl
          curl -s https://raw.githubusercontent.com/swiftlang/github-workflows/refs/heads/main/.github/workflows/scripts/check-swift-format.sh | bash